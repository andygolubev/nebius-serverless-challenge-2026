# Sim2Policy package

The application package for the repository-level Sim2Policy tutorial. Run `uv sync --extra dev`
from this directory for shared development, or include `--extra sb3` / `--extra mjx` for a backend.

