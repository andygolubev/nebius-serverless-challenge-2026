# Private debug infrastructure

This directory contains non-submission debugging infrastructure used during development. It is not
part of the Nebius challenge operator path, and public challenge documentation must not rely on it.

For private continuation notes, connection details, and teardown commands, use the gitignored
repository-root `IMPLEMENTATION_LOG.MD`.
